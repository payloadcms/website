import React from 'react'

import Meta from '@components/Meta'

export default async () => {
  return (
    <React.Fragment>
      <Meta title="Teams | Payload Cloud | Payload CMS" slug="cloud/teams" />
    </React.Fragment>
  )
}
