import React from 'react'

import Meta from '@components/Meta'

export default async () => {
  return (
    <React.Fragment>
      <Meta title="Reset password | Payload Cloud | Payload CMS" slug="reset-password" />
    </React.Fragment>
  )
}
