import React from 'react'

import Meta from '@components/Meta'

export default async () => {
  return (
    <React.Fragment>
      <Meta title="Login | Payload Cloud | Payload CMS" slug="login" />
    </React.Fragment>
  )
}
