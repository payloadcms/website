import { Metadata } from 'next'

import { HoverHighlightsPage } from './client_page'

export default () => {
  return <HoverHighlightsPage />
}

export const metadata: Metadata = {
  title: 'Hover Highlights',
}
