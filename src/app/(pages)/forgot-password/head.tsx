import React from 'react'

import Meta from '@components/Meta'

export default async () => {
  return (
    <React.Fragment>
      <Meta title="Forgot password | Payload Cloud | Payload CMS" slug="forgot-password" />
    </React.Fragment>
  )
}
